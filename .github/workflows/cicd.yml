name: ci build and bush docker image to ECR
on:
  push:
    branches:
      - main
  
jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v2
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: us-east-1

      # - name: Login to Amazon ECR
      #   run: |
      #      aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 744531146604.dkr.ecr.us-east-1.amazonaws.com
      
      # - name: Build and push image to Amazon ECR
      #   run: |
      #       docker build -t mediplus:v1 .
      #       docker tag mediplus:v1 744531146604.dkr.ecr.us-east-1.amazonaws.com/mediplus:v1
      #       docker push 744531146604.dkr.ecr.us-east-1.amazonaws.com/mediplus:v1


  deploy:
    runs-on: ubuntu-latest
    needs: build-push
    steps:
      - name: check my ssh key
        run: |
          echo "${{secrets.SSH_KEY}}" > ssh_key.pem
          chmod 600 ssh_key.pem
          cat ssh_key.pem
      
      
